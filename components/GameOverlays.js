/*
import React from 'react';
import { View, Text, Button } from 'react-native';
import {styles} from "./AllStyles";

// Composant pour afficher l'écran de fin de partie
export function GameOverOverlay({ score, startGame, username }) {
    return (
        <View style={styles.overlayContainer}>
            <Text style={styles.overlayText}>Game Over</Text>
            <Text style={styles.finalScore}>{username}, your final score is: {score}</Text>
            <Button onPress={startGame} title="Play Again"/>
        </View>
    );
}

// Composant pour afficher l'écran de pause
export function GamePausedOverlay({ togglePause }) {
    return (
        <View style={styles.overlayContainer}>
            <Text style={styles.overlayText}>Pause</Text>
            <Button onPress={togglePause} title="Resume"/>
        </View>
    );
}*/
